# El frontend NO requiere variables de entorno en desarrollo:
# la llamada a /api la resuelve el proxy de Vite (dev) o de nginx (producción).
#
# Este archivo se deja como plantilla por si en el futuro se necesitan flags VITE_*.
# (Las variables expuestas al navegador deben llevar el prefijo VITE_).
